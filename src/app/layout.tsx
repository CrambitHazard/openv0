import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'OpenV0 - AI-Powered Web App Generator',
  description: 'Transform your ideas into fully functional websites with the power of AI',
  keywords: ['AI', 'web development', 'code generation', 'OpenRouter', 'DeepSeek'],
  authors: [{ name: 'OpenV0 Team' }],
  viewport: 'width=device-width, initial-scale=1',
  robots: 'index, follow',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  )
}
